{
  "name": "blunder",
  "version": "1.0.0",
  "description": "Pathfinding solver with dynamic obstacles and state-dependent mechanics",
  "main": "main.ts",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}
